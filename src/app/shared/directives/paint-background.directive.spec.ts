import { PaintBackgroundDirective } from './paint-background.directive';

describe('PaintBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new PaintBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
